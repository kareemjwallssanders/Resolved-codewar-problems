// Morning Challenge: Create an object via a constructor that has two properties 
// that are numbers and a method. That method must call a function 
// that calls a function. That final function must multiply the first 
// number by itself x number of times where x is the second number 
// of the original object


function sneakerBox(){
    let newTotal = 0;
    for(let count = 1; count <= k; i++){
        newTotal = newTotal + i*i
    }
    console.log(newTotal)
}

function justdoit(x,y){
sneakerBox(x.y)
}


//Create an object via a constructor that has two properties 

function Multinum(numbers){
    this.numbers= 5;
    this.numbers= 6;
    this.numbers= numbers * numbers ;
    this.multiply= function(){
        return number * number;
        console.log(); 
    }

}

let Create1 = ;
let Create1 = ;
let Create1 = ;



// function square(number) {
//     
//   }
  